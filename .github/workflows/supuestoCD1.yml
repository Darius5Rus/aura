name: CD_Supuesto 1

# workflow_dispatch añade un botón "Run workflow" en la pestaña Actions de GitHub
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Generar paquete con Maven
        run: mvn -B clean package -DskipTests